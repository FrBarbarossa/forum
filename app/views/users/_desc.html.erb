<%= form_with url: '/description' do |form| %>
    <div id='errors_desc'></div>
    <%= form.text_area :description, class: 'form-control', maxlength: 200, value: current_user.account.description  %>
    <p><%= form.submit("Изменить описание", class: 'form-control', id: 'btn') %></p>
<% end %>